# ===== Turso 数据库 =====
TURSO_DATABASE_URL=libsql://qingtao-fan.turso.io
TURSO_AUTH_TOKEN=替换为你的实际 token

# ===== Vercel Blob（可选；启用后 /api/upload 走云对象存储而非 base64 入库）=====
# 在 Vercel 控制台 → Storage → Create Blob Store，绑定项目后会自动注入。
# 本地调试可手动粘贴 token。
BLOB_READ_WRITE_TOKEN=